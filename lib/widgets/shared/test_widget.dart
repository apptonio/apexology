import 'package:flutter/material.dart';

class TestSharedWidget extends StatelessWidget {
  const TestSharedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
